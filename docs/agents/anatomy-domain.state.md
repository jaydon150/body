# Agent state: anatomy-domain

Append-only state log. Most recent at top.

**Initialized:** 2026-05-11
**Last invocation:** 2026-05-11 (Phase 1 step P1.07 — cross-domain dispatch with Asset Pipeline)

---

## Open items

- 2026-05-11 (P1.07) | **Sternum composite shape decision deferred to P1.08 with Anatomy Domain sign-off.** `UBERON:0000975` (sternum) has no mesh on disk but its 3 constitutional children (manubrium `UBERON:0002205`, body of sternum `UBERON:0006820`, xiphoid process `UBERON:0002207`) all have full LOD0/LOD1/LOD2 chains. The composite is recoverable at the registry level. P1.08 must decide between extending `app/shared/schema/mesh-asset-manifest.json` with an optional `composite` block (preferred -- semantics on disk) vs. runtime alias resolution. Anatomy Domain agrees with synthesis (per P1.07 cross-domain dispatch); the open item is the schema-shape question routed to Architect.
- 2026-05-11 (P1.07) | **No other composite-synthesis opportunities in the dataset.** The 29 gap structures are all LEAF sub-structures of meshes that exist as wholes (e.g. head/neck/shaft of femur are parts of the femur mesh, not parents whose parts have meshes). These are decomposition candidates for P2 procedural splits, not composition candidates for P1.08. Sternum is the unique exception.
- 2026-05-11 (P1.07) | **DAG coherence + schema-conformance for `nodes.json` + `relations.json` confirmed**: 125 nodes / 125 edges / 0 cycles / 0 duplicate ids / 0 referential-integrity failures / 0 schema-shape failures. Every node passes `anatomical-id-schema.json` `node` $def; every edge passes `edge` $def. No `kind` outside the enum, no `status` outside the enum, no language code outside the BCP-47-ish pattern. The full Ajv-based validation pass at P1.09 is still owed but P1.07's hand-rolled shape check is a solid baseline.

- 2026-05-11 | Schema gap: `anatomical-id-schema.json` has no shape for `synonyms.json` content. The file's required top-level fields (`version`, `nodes`, `edges` with `additionalProperties: false`) leave no place to store standalone synonym maps. Current workaround: vernacular EN synonyms (breastbone, collarbone, kneecap, shinbone, calf bone, heel bone, jawbone, lower jaw) added directly to `nodes.json[].labels[]` with `source: "vernacular"`; `synonyms.json` is a schema-valid placeholder (empty nodes + empty edges) until the schema grows a synonym shape. Architect needs to decide: (a) add a `$defs.synonym_entry` and an optional top-level `synonyms` array, or (b) keep all synonym data inside `labels[]` and remove `synonyms.json` from the agent contract. Filed back to Orchestrator.
- 2026-05-11 | UBERON release version still unpinned — same open item the Research/Docs agent flagged. OBO Foundry page does not surface a release date; the GitHub releases query is still owed. Once an upstream version is anchored, the 2026-05-11 timestamp on `nodes.json`/`relations.json` should track to it.
- 2026-05-11 | TA2 Latin synonyms for several rows (mandible, vomer, scapula, tibia, fibula, etc.) were taken from `labels[]` with `source: "TA2"` but UBERON's OLS4 response did not surface them tagged as Latin synonyms — they were taken from standard TA2 nomenclature. A formal TA2 verification pass (against the FIPAT TA2 source rather than UBERON's synonym field) should run before anatomist promotion to `reviewed`.
- 2026-05-11 | Phase 1 spec mentions skull foramina (foramen magnum, ovale, rotundum, jugular) as "tentative" Phase 1 nodes. Not looked up in this pass — Phase 1 acceptance criterion #2 says ~80-120 nodes and this pass already lands at 125. Foramina deferred to a later batch; flag for anatomist when reviewing whether to add now.
- 2026-05-11 | Radius/ulna sub-structures (head, neck, shaft, styloid processes), tibia/fibula sub-structures (medial/lateral malleolus, tibial plateau, head of fibula), individual metacarpals/metatarsals 1-5, and individual carpal-bone variants per-side were not added in this pass. ~25-40 additional nodes possible if Phase 1 needs them.
- 2026-05-11 | `relations.json` does not assert `systemic_part_of skeletal-system` from every leaf bone — only from the femur seed (per dispatch instruction) and structurally from the top-level subdivisions. Transitively every bone reaches `UBERON:0001434` via `regional_part_of` chains. If consumers (sidebar tree, peel) need a direct `systemic_part_of` edge for every node, that's another ~100 edges of mechanical addition.
- 2026-05-11 | UBERON:0001272 ("hip bone") was modeled as `regional_part_of pelvic girdle skeleton` rather than being subsumed by it. Ilium/ischium/pubis are `constitutional_part_of` hip bone. This matches FMA convention and TA2 register but the alternative (ilium directly under pelvic girdle skeleton) is sometimes seen. Anatomist call.

## Decisions log

- 2026-05-11 (P1.07) | **Cross-domain dispatch with Asset Pipeline confirmed the canonical store + ontology are coherent.** 8/8 validation checks PASS. The validator at `pipelines/04-validate-ontology/validate.mjs` and report at `pipelines/04-validate-ontology/validation-report-2026-05-11.md` are the deliverables. Anatomy Domain's contribution to this dispatch: (a) reviewed ontology DAG coherence, schema-conformance, and gap-report reconciliation; (b) signed off on the sternum composite-synthesis approach for P1.08 (3 children → 1 composite registry entry); (c) confirmed no other composite-synthesis opportunities exist in the current 125-node dataset.
- 2026-05-11 (P1.07) | **Sternum composite is recoverable; manubrium / body-of-sternum / xiphoid-process all have complete LOD0/LOD1/LOD2 chains with valid attribution.** Confirmed UBERON IDs of the 3 children: `UBERON:0002205` (manubrium), `UBERON:0006820` (body of sternum), `UBERON:0002207` (xiphoid process). P1.08 should synthesize `UBERON:0000975` (sternum) at the registry level by referencing these 3. Anatomy Domain endorses option (a) -- extend `mesh-asset-manifest.json` with an optional `composite` block -- over option (b) (runtime-only alias), because option (a) keeps the composite semantics auditable in the manifest. Final shape decision is Architect's call.
- 2026-05-11 (P1.07) | **The femur seed (UBERON:0000981) still has both the procedural proxy in `data/derived/mesh-registry.json` AND the real BP3D-derived mesh at `data/canonical/meshes/uberon_0000981/`.** The real mesh has the correct FMA alias (FMA:9611), the verbatim BodyParts3D copyright string, and a 3-entry edits chain (convert + merge + cleanup) at LOD0. P1.08 promotes the real mesh to primary and demotes (or removes) the procedural proxy in the rebuilt registry. The procedural-proxy code in `app/web/src/scene/FemurScene.tsx` and `app/web/src/scene/anatomySeed.ts` retirement is P1.10's concern, not P1.08's.

- 2026-05-11 | Skeletal sub-ontology depth: split named bones per Phase 1 spec (every cranial bone, every facial bone, every cervical vertebra, every thoracic vertebra, every lumbar vertebra, every rib, every carpal, every tarsal); grouped only the metacarpals (UBERON:0002374) and metatarsals (UBERON:0001448) as a single region-class node each (parent class). Phalanges of manus/pes split by row (proximal / middle / distal) but not per-digit. Total: 125 nodes covering all top-level subdivisions and named bones, plus flagship-bone sub-structures for femur (head, neck, shaft, both trochanters, both condyles) and humerus (head, neck, shaft, trochlea, capitulum, both epicondyles) and scapula (glenoid, acromion, coracoid, spine).
- 2026-05-11 | Label register: `labels[0]` is TA2 English (e.g. "atlas (C1)", "frontal bone", "hip bone", "zygomatic bone", "scaphoid bone", "trapezium", "medial cuneiform bone") rather than UBERON's comparative-anatomy register ("vertebral bone 1", "tetrapod frontal bone", "innominate bone", "jugal bone", "radiale", "distal carpal bone 1", "distal tarsal bone 1"). `labels[1]` is Latin from TA2. UBERON's preferred English label is preserved as a third entry with `source: "UBERON"` only when it materially diverges from TA2 — otherwise omitted to avoid label-array bloat.
- 2026-05-11 | Vernacular EN synonyms (breastbone, collarbone, kneecap, shinbone, calf bone, heel bone, jawbone, lower jaw) included on `nodes.json[].labels[]` with `source: "vernacular"` rather than in `synonyms.json`. Reasoning: the schema's top-level shape (`version`/`nodes`/`edges` only, `additionalProperties: false`) provides no synonym container, and the `label` $def already supports the `source` field cleanly. Open item filed for Architect to grow a synonym shape if a separate file is mandated.
- 2026-05-11 | Second-pass batch for the 7 inferred-by-pattern rows resolved: thoracic T4 (UBERON:0004629 / FMA:9248), T5 (UBERON:0004630 / FMA:9922), T7 (UBERON:0004632 / FMA:9968), T9 (UBERON:0004633 / FMA:10014), T10 (UBERON:0004634 / FMA:10037), T11 (UBERON:0004635 / FMA:10059). **Sharp edge:** thoracic T8 does NOT sit at UBERON:0004633 (the pattern inferred) — it sits at UBERON:0011050 / FMA:9991, a NON-CONTIGUOUS UBERON ID exactly like rib 8 at UBERON:0010757. Confirmed via direct OLS4 search. The pattern inferred T8=0004633 but 0004633 is actually T9. The P1.01 crosswalk note flagging "T4-T5, T7-T11 inferred-by-pattern" should be updated upstream. Ribs 4 (FMA:7749), 5 (FMA:7776), 6 (FMA:8147) verified clean at the expected UBERON:0004604-0004606.
- 2026-05-11 | Flagship-bone sub-structures all resolved cleanly in UBERON; no BODY:NNNN IDs needed for Phase 1 skeletal. Femur: head (UBERON:0006767), neck (UBERON:0007119), shaft (UBERON:0006862, UBERON's label "diaphysis of femur"), greater trochanter (UBERON:0002503), lesser trochanter (UBERON:0002504), medial condyle (UBERON:0009984), lateral condyle (UBERON:0009985). Humerus: head (UBERON:0006801, UBERON's label "proximal head of humerus"), neck (UBERON:4200172 — note the non-7-digit-but-still-7-digit form, still validates the `UBERON:\\d{7}` pattern), shaft (UBERON:0004652 "humerus diaphysis"), trochlea (UBERON:0000144), capitulum (UBERON:0010853), medial epicondyle (UBERON:0006806 "entepicondyle of humerus"), lateral epicondyle (UBERON:0006807 "ectepicondyle of humerus"). Scapula sub-parts: glenoid cavity (UBERON:0006657), acromion (UBERON:0002497), coracoid process (UBERON:0006633), spine of scapula (UBERON:0004651). All verified directly via OLS4 with FMA xrefs.
- 2026-05-11 | Zero BODY:NNNN IDs assigned in this pass. UBERON's skeletal coverage was complete for every Phase 1 target. Counter-sign with the P1.01 crosswalk's confidence note: skeletal anatomy is the easy domain for UBERON; later phases (functional, vascular, neural) will likely surface BODY:NNNN candidates.
- 2026-05-11 | Femur seed (UBERON:0000981 with FMA:9611, ta2:1360, snomed:71341001, TA2 EN "Femur" + Latin "os femoris") preserved verbatim. Per dispatch, added `regional_part_of` UBERON:0001441 (lower limb skeleton) and `systemic_part_of` UBERON:0001434 (skeletal system) edges. The systemic edge carries `source: "hand-curated"` because UBERON itself only asserts `BFO:0000050` (part of) relationships, not the FMA-style systemic/regional split.
- 2026-05-11 | Graph shape: 125 nodes (1 system, 16 region, 108 structure), 125 edges. Edge type counts (audited from relations.json): regional_part_of=70, member_of=30, constitutional_part_of=24, systemic_part_of=1. Cycle-checked via DFS; no cycles. Multi-parent shape verified: leaf nodes like UBERON:0001092 (C1) have both `member_of cervical vertebrae set` AND transitively reach `vertebral column` via the set's `regional_part_of` edge — DAG semantics correct.
- 2026-05-11 | Validation status: ad-hoc data-against-schema validator (not committed) passed structural and referential checks. `npm run validate:schemas` shows 7/7 schemas pass. `npm run verify` (typecheck + schemas + build) green; vite build emits 168 kB gzipped index bundle. Phase 0 schema validator passes are baseline; Phase 1 step P1.09 will add full ajv-based meta-and-data validation.

## Handoffs

- 2026-05-11 (P1.07) → Asset Pipeline (P1.08): Anatomy Domain has signed off on the sternum composite-synthesis approach. The 3 child UBERON ids (`0002205` manubrium / `0006820` body / `0002207` xiphoid process) have full LOD chains. P1.08 should: (a) synthesize the `UBERON:0000975` sternum registry entry from the 3 children, ideally via a new optional `composite` block in `mesh-asset-manifest.json` (Architect call); (b) replace the procedural-femur-proxy registry entry with the real BP3D-derived `data/canonical/meshes/uberon_0000981/lod0|1|2.glb` set. Both items are explicit instructions in the P1.07 validation report.
- 2026-05-11 (P1.07) → Architect: schema-extension question — extend `app/shared/schema/mesh-asset-manifest.json` with an optional `composite` block (preferred per P1.07) vs. push composite resolution into the loader. Filed for Architect decision before P1.08 commits its registry rebuild. Either path is supportable; option (a) keeps semantics auditable in the manifest, option (b) keeps the schema unchanged.

- 2026-05-11 → Asset Pipeline: skeletal sub-ontology delivered at `data/canonical/ontology/{nodes.json,relations.json,synonyms.json}` (version 2026-05-11). 125 nodes covering the full Phase 1 skeletal scope: 1 system, 10 regions (axial, appendicular, skull, vertebral column, thoracic cage, pectoral/pelvic girdles, upper/lower limb skeletons, cervical/thoracic/lumbar vertebra sets, metacarpals/metatarsals/phalanges of manus/pes as parent regions), 1 femur + sub-structures, and ~110 named bones / sub-bones. Every node carries a UBERON primary ID and an FMA alias; every label[0] is in TA2 English register with Latin in label[1]. Asset Pipeline at step P1.07 (`pipelines/04-validate-ontology`) will cross-check that every BodyParts3D mesh resolves to one of these IDs and surface gaps back to Anatomy Domain. **Three sharp edges to watch:** (1) rib 8 sits at UBERON:0010757 (non-contiguous from UBERON:0004601-0004611 sequence); (2) thoracic T8 sits at UBERON:0011050 (non-contiguous from UBERON:0004626-0004636 sequence); (3) neck of humerus at UBERON:4200172 (non-zero high-prefix UBERON ID — still 7 digits, still pattern-valid). Do not assume rib/vertebra IDs are sequential during mesh registry generation; consult `nodes.json` as source of truth.
- 2026-05-11 → Content: TA2 English labels in `nodes.json` are the authoritative display register for content records. Vernacular EN synonyms ("breastbone", "collarbone", "kneecap", "shinbone", "calf bone", "heel bone", "jawbone", "lower jaw") live in `labels[]` with `source: "vernacular"`. Content can lean on these for search and intro-paragraph naming.
- 2026-05-11 → UI: every label[0] is the recommended display string. The full `labels[]` array gives the search index a row per language and a row per register; vernacular labels are tagged for "informal mode" search hits.
- 2026-05-11 → Architect / Orchestrator: schema gap for `synonyms.json` flagged in Open items. Current file shape is a schema-valid placeholder; if a richer synonym store is needed before Phase 2, the `anatomical-id-schema.json` needs a $defs.synonym_entry addition.

## Invocation history

- 2026-05-11 | **Third invocation. Phase 1 step P1.07 dispatch (cross-domain with Asset Pipeline).** Single read-mostly invocation operating as both Asset Pipeline and Anatomy Domain. Consumed both agent prompts + both state files + ADRs 0001/0004/0006/0007 + the gap report + the canonical store. Produced the 8-check validation report at `pipelines/04-validate-ontology/validation-report-2026-05-11.md`. Anatomy Domain contributions: confirmed DAG coherence (125 nodes / 125 edges / 0 cycles / 0 schema-shape failures), confirmed gap-report reconciliation is clean both directions, signed off on the sternum composite-synthesis approach for P1.08 with the 3 confirmed child UBERON ids (0002205 / 0006820 / 0002207). Zero ontology mutations -- read-only invocation. Overall verdict: **PASS, P1.08 unblocked**. Open items + handoffs filed above. `npm run verify` in `app/web/` green post-change.
- 2026-05-11 | Second invocation. **Phase 1 step P1.02 dispatch** from Orchestrator: draft the skeletal-system sub-ontology. Consumed `docs/references/summaries/uberon-fma-skeletal-crosswalk.md` (P1.01 deliverable from Research/Docs), extended the user's femur seed (UBERON:0000981), ran the mechanical second-pass batch for the 7 inferred-by-pattern rows (T4-T5, T7-T11 thoracics; ribs 4-6) — discovered T8 sits at UBERON:0011050 (non-contiguous, like rib 8), corrected the pattern. Looked up flagship-bone sub-structures via OLS4 for femur (7 sub-parts), humerus (7 sub-parts), scapula (4 sub-parts); zero BODY:NNNN IDs assigned. Wrote `data/canonical/ontology/{nodes.json,relations.json,synonyms.json}` at version 2026-05-11. 125 nodes, 125 edges, 0 cycles, all referential integrity OK. Schema validation (`npm run validate:schemas`) green; full `npm run verify` (typecheck + schemas + build) green; vite production build 168 kB gzipped. Handoffs registered above.
- 2026-05-11 | First invocation (initialization only). Phase 0 close created the agent state log scaffold; no data work in that invocation.
</content>
